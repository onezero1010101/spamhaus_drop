#on:
#  schedule:
#   - cron: "5 * * * *"
#jobs:
#  build:
#    runs-on: ubuntu-latest
#    steps:
#      - uses: actions/checkout@v4
#        with:
#          fetch-depth: 0
#      - name: Run bash script
#        run: bash spamhaus.sh
on: push
jobs:
  wget:
    runs-on: ubuntu-latest
    steps:
    - name: wget
      uses: wei/wget@v1
      with:
        args: -O drop_v4.json -P / https://www.spamhaus.org/drop/drop_v4.json
